<!-- markdownlint-disable MD024 -->
<!-- markdownlint-disable MD025 -->
<!-- markdownlint-disable MD041 -->
# ChangeLog

## [v0.3.0] - 2026-08

### 🌐 MCP 唯一动作通道 + 身份注册 + 独立 WebUI（v4.1 完整落地）

人类玩家与 agent 走同一条 MCP 动作通道（B10），REST 仅非动作；身份自助注册（B13）；WebUI 移动端优先（本身是 MCP 客户端）。

- **进程内 MCP server（`world/mcp/`）**：
  - 7 个世界工具（world_look / world_move / world_say / world_bag / world_use / world_interact / world_who），返回结构化 JSON `{text, ui, effects}`（agent 读 text、UI 渲染 ui）。
  - **streamable HTTP**（公网接入）：FastMCP `streamable_http_app()` + 认证中间件 + 独立 uvicorn 服务（配置 `enable_world_api` / `world_api_host` / `world_api_port`，默认 6288）；连接即身份验证——中间件校验 `Bearer <token>` 并把 `{entity_id, tier}` 注入 JSON-RPC `params._meta`，工具经 `ctx.request_context.meta` 读取。
  - **stdio**（本地）：独立进程入口 `python -m astrbot_plugin_worlditor.world.mcp.stdio --db <world.db> --token <凭据>`，一个连接绑定一个实体（修复 Windows stdout GBK 编码）。
  - 真实 uvicorn 下 MCP HTTP 端到端测试（认证 + 注入 + 工具执行 + 交互）。
- **身份注册系统（`world/identity.py` + accounts/tokens/invite_codes 表）**：
  - auth_mode 三模式（open / invite / closed）+ token 三档（read / play / admin）+ admin_key 管理员通道（豁免模式限制）。
  - 人类账户（用户名+密码，PBKDF2）→ player 实体；agent 注册 → agent 实体 + 凭据；邀请码批量生成/吊销；改密/注销/吊销；read 档围观免注册。
- **世界服务（6288 独立端口，WebUI 单一连接源）**：`/world/mcp`（动作）+ `/state` `/scene`（含实体可用动作）`/bag`（快照）+ `/events`（SSE，事件总线订阅出口）+ `/auth/*`（身份）+ `/plays/<id>/web/*`（玩法包资源）+ CORS（`allowed_origins`）。
- **界面扩展（B9）**：`engine.apply_ui_hooks`（before/after/replace 递归展开，provider 异常隔离），MCP 交互返回前应用——玩法包注册的钩子对 agent 与 WebUI 同时生效。
- **v4 引擎**：补齐地图编辑原语（地块 create/update/delete/move、连接槽位、地图 create/update、实体字段更新）+ 事件流订阅（subscribe/unsubscribe，队列满丢最旧）。
- **WebUI（`webui/`，Vue3 + Vite，移动端优先）**：hash 路由四页（世界地图 / 背包 / 角色 / 日志）+ 登录注册页（含围观 read 档与 agent 凭据申请）；**轻量 MCP client**（fetch JSON-RPC + session + _meta）；世界页触屏 SVG 网格地图（平移/缩放/方向按钮/说话/同地块角色条）+ SSE 增量更新与断线重连快照兜底；交互弹窗按 UiBlock schema 渲染（text/menu/list/form/confirm/character/custom fallback）。`npm run build` 通过。
- **插件内置托管 WebUI**：`webui/dist/` 构建产物随插件发行，6288 世界服务自动挂载为根路径静态资源（免认证加载登录页，API 路由优先）——开启 `enable_world_api` 后访问 `http://<主机>:6288/` 即完整 WebUI，无需单独部署前端；也可独立部署（`VITE_WORLD_API` + `allowed_origins`）。
- 插件配置（`_conf_schema.json`）：auth_mode / admin_key / allow_agent_register / enable_world_api / world_api_host / world_api_port / allowed_origins。
- **版本统一 v0.3.0**（metadata / main.py / README / CHANGELOG；0.x 预发布，正式上线前保持 0 开头）。
- 测试：+8 世界服务（含真实 uvicorn MCP 端到端）+ ui_hooks，全套 158 通过。

### 🏗 世界底子内核（v4 起点：事实模型 + 原语 + 注册表 + 事件总线）

定位从「世界编辑器」升级为「世界平台内核」：内核不做玩法，玩法由玩法包
（`worlditor_play_*`）以注册表接入生长。v4 为破坏性重构（无迁移），与 v3
**同库共存过渡**（同一 world.db：v3 表结构沿用共享，v4 新增五表独立；
v3 功能（LLM 工具 / 调试页）继续可用，v4.1 起取代 v3）。

- **实体统一模型（B12）**：世界只有地块与实体两个概念——玩家（kind=player）、
  agent（kind=agent）是内置身份化实体，其余 kind（merchant/sign/door...）由
  玩法包注册；统一 `entities` 表（uuid4 id / 位置 / attrs / state / user_id /
  last_active_ts），身份化实体有背包、位置持久化。
- **物品（定义与持有分离）**：`items` 表（ItemDef：name/desc/icon/stackable/
  use_action/attrs）+ `inventories` 表（按实体持有，条目可带个体差异 attrs，
  C1 装备/格子由玩法包自管）；原语 give/take/count/list。
- **交互协议（A1）**：`interact(entity, target, action, args, item_id)` →
  玩法包 handler（async，兼容同步）→ 内核结算**声明式 effects**（give_item /
  take_item / move / move_entity / set_attrs / say 子集，无 teleport）→
  `InteractionResult{text, ui, effects}`（B10 返回协议）。
- **事件总线（单一事件源）**：9 个事件（on_tick 带各自间隔 / on_entity_move /
  on_entity_enter / on_say / on_interact / on_item_used / on_entity_removed /
  on_entity_changed / on_world_edited），玩法包订阅 + 写入 world_log（上限
  5000 条，B3）；on_tick 单循环 1s 粒度调度、串行执行、异常隔离（A3）。
- **广播（B2）**：内置喇叭道具，say(scope=world) 消耗 1 个 + 每人 30s 冷却
  （管理员豁免）；cell 级不限。
- **玩法包系统（B6）**：自动发现 `demo_play/`（内核自带，SDK 模板）与
  `<数据目录>/plays/worlditor_play_*`；play.yaml 元数据 + requires 校验
  （v4.0 只校验 worlditor 版本，plays 依赖声明保留、解析 v4.2）；
  每个玩法包独立 `WorlditorPlayAPI`（kv namespace 隔离）；加载失败记日志
  跳过、不阻断内核；`setup(api, context)` / 可选 `teardown(api)`。
- **实体移动**：身份化实体按路径移动（v3 语义：死引用剔除 + 加权抽目标，
  返回 SceneView）；block_move 阻挡（state 可动态覆盖 kind 声明，门开/关
  由玩法包写 state）；`move_entity` 直接坐标（行为驱动，传送语义）。
- **地图编辑**：`place_entity` / `remove_entity`（admin，B8：实体 = 地图
  编辑内容，无生成/移除原语给玩法包）；删除地块级联删除其上实体。
- **种子世界 v4**：41 地块小镇沿用；新增演示实体（广场「商贩·阿福」
  merchant / 步行街「告示牌」sign / 迷雾森林入口「木门」door block_move）
  与演示物品（苹果 use_action=eat / 喇叭内置广播道具）。
- **内置 demo_play/**：演示 item / entity_kind / interaction / event 完整
  链路（talk/trade 对话流、effects 声明式 vs 命令式 API 两种购买写法、
  苹果食用回血事件链、告示牌 tick 更新、迷雾进入提示），充当 SDK 模板，
  可删除。
- **版本统一**：v3 版本漂移（metadata v0.2.0）修复为 v0.3.0。
- 并发：实例级可重入异步锁（AsyncRLock，事件/tick handler 内调原语不死锁）；
  时钟/PRNG 注入保证可测（46 个新单测，全套 117 个通过）。

### 💥 破坏性变更（相对 v3 设计）

- v4 数据模型为**新表**（entities/items/inventories/play_data/world_log），
  与 v3 表同库并存、无迁移；玩法包为 v4 的玩法入口（v4.1 起 WebUI + MCP）。

## [v3.0.0] - 2026-08-13

### 💥 数据模型重构 v3（破坏性变更，无迁移）

世界底座从「id 关联的有向图（Location / Exit 独立实体）」重构为「以 (map_id, 行, 列) 为身份的地块 + 内嵌固定 4 方向连接槽位 + 分时段加权文本（TextSchedule）+ 地块模板」。

- **无迁移**：solo 迭代、未公开，v2 旧库（`locations` / `exits` 表）数据直接丢弃，空库按新模型幂等重建播种世界（网格小镇：广场 · 步行街 · AstrBot大道 · 开源小区 · AstrBot大学 · 迷雾森林，连接由占位网格自动生成）。
- **播种调整：商铺只连大道**：移除插件商店地块（41 个地块）；大道两侧 7 家商铺（插件市场 / Skill / 人格设定 / 知识库 / TTS / T2I 市场、旧书店）只与 AstrBot大道双向连接，不与老路 / 步行街 / 其他商铺相连（连接生成规则在占位网格自动双向的基础上追加 `avenue_only` 约束）。
- **地块身份 = (map_id, row, col)**：引入 `maps` 表（本次仅单图 `default`），地块组合主键；坐标只读，新增「移动地块」工具（原子重写指向旧坐标的全部连接目标与地块上玩家位置，目标格被占则拒绝）。
- **连接并入地块**：不再有独立 Exit 实体——每地块固定 4 个方向槽位（up/right/down/left），槽位内 `paths` 多条**平行路径**（可增删 / 排序），每条路径 `label`（TextSchedule）+ `reveal_target` + `targets` 有序列表（**首个 = 主目标**，其余 = 意外路径，移动时加权随机命中）。总出度不限（4 方向 × 多路径 × 多目标）。
- **TextSchedule 分时段加权文本**：描述与路径标签按每日钟点窗口（可跨午夜）+ 权重抽取当前文本；地图级时区（None = 服务器本地）。
- **移动接口重构**：`world_move` 从按 `exit_id` 改为按「方向 + 路径索引」（`world_look` 每次重列路径索引，场景内有效）；`POST /world/move` 参数改为 `{player_id, direction, path?, target?}`。
- **死引用规则（路径级）**：路径主目标不可解析 → 整条路径死（不展示 / 不可选）；路径内意外目标不可解析 → 静默跳过；槽位启用但全部路径死 → 视为禁用；前端以红 / 虚线区分「显式禁用」与「死引用」。
- **模板 = 复制预设**：模板 CRUD + 应用到空地块；同图目标存方向相对偏移（放置时平移），跨图目标存绝对坐标原样复制。
- **Web API 重构**：`POST /world/location/{create,update,delete,move}`（坐标不再可改）、`POST /world/connection/update`（编辑方向槽位）、`POST /world/template/{create,update,delete,apply}`。
- **前端调试页全量重写**：编辑视图点选间隙编辑两侧槽位、平行路径标红虚线、模板应用；玩家视图十字棋盘按方向槽位逐一列出平行路径；新增 TextSchedule 编辑器（默认纯文本，高级折叠多时段 / 权重）与路径编辑器（增删 / 排序 / 权重）。
- **删除级联**：主目标指向被删地块 → 整条路径删除；意外目标 → 仅删该目标；拒绝删除有玩家占据的地块。

### 🐛 修复编辑视图连接绘制异常（缺失 + 错位）

- **修复间隙锚点取自来源地块**：`collectGapStubs` 把每个间隙的锚点记录为**来源地块**坐标，但 `up` / `left` 连接的间隙位于来源地块的上一格 / 左一格（key 为 `v:row-1` / `h:col-1`），导致含 up / left 的整个间隙（连同其 down / right 线）被绘制到相邻下一格的位置——正确位置上的连接消失（部分存在的连接不绘制）、相邻间隙出现错位 / 重叠的幽灵线（部分绘制的连接位置异常）；修复为锚点取间隙 key 自身的 `(row, col)`，与来源地块无关（62 个种子间隙全部对齐、115 条连接线几何验证全部正确）。
- **修复平行路径线均布区间**：平行路径线沿间隙条的均布区间从 `CELL`（120px）误改为 `GAP`（46px），导致同一间隙内的多条连接线全部簇拥在间隙条前 46px（约前 1/3）处、未在间隙条全程居中均布——双向连接的两条线挤在左上角、多平行路径互相堆叠；恢复为按 `CELL` 全程均布（单线居中，n 线等距 120/(n+1)px 错开）。

## [Unreleased] - 2026-08-11

### 🐛 修复全图缩略图随地图滚动滚出可视区

- **缩略图改为 fixed 定位**：此前缩略图是滚动容器 `#graph` 内的绝对定位元素，地图平移 / Ctrl 缩放改变 `scrollLeft/Top` 时会随内容一起滚动，被滚到视图不可见的区域；改为锚定视口的 `position: fixed`，`left/top` 按图区域右下角计算，拖动位置跨重绘保留、窗口缩放自动跟随。

### 🛠 编辑视图主视图重构：连接改为间隙连线 + 固定边界 + 右键拖动 + 全图缩略图

- **地块是唯一的「格」，连接不以格呈现**：移除连接块 / 占位小格，连接改为绘制在地块间隙（`GAP=46`）中的 SVG 连线——`a→b` 单向带箭头、`a↔b` 双向无箭头连线、`a→b′` 非常规连接（目的地非方向相邻）带箭头虚线 + 末端小标记格。
- **相邻地块可同时显示两条连接**：如 `a→b` 普通 + `b→c′` 非常规（或两侧都是非常规），各自独立绘制、沿间隙垂直方向均布错开，不再合并遮蔽。
- **地图固定大小**：内容边界外任意方向再延伸 `EDGE=10` 个空地块，可平移范围固定；查看模式只显示已存在地块与连接，编辑模式显示全部空地块（点击新建）与网格背景。
- **隐藏滚动条 + 右键拖动**：视图隐藏横竖滚动条，右键按住拖动平移（滚轮 / Shift+滚轮平移兜底），Ctrl/⌘+滚轮以光标为中心缩放。
- **全图缩略图**：右下角缩略图显示全图（地块分布）与当前视口范围，可收起/展开、可拖动（标题栏移动位置），点击或拖动可跳转视口。
- 点击交互调整：单条连接线点击直接选中该出口；双向合并线 / 多边同线点击选中整个间隙（查看 / 编辑块内出口）。

### 🐛 修复非常规连接 right / down 方向渲染为全间隙无箭头线

- **修复 right / down 方向非常规连接绘制错误**：`drawLine` 的分支条件 `line.kind === "bi" || line.side === "r"` / `"d"` 会先命中 right / down 的非常规连接，导致其画成全间隙无箭头线（无箭头、无末端标记格），与双向合并线无法区分；改为仅「双向合并线」与「同方向普通单线」进入该分支，非常规连接始终走「箭头 + 末端小标记格」分支（此前 left / up 方向仅因控制流巧合才正确绘制）。

### 🛠 编辑视图：地块放大 + 连接块变窄 + 占位格无效化样式 + 空连接块不可点

- **地块更大 / 连接块更窄**：地块轨道 `CELL` 88→120px（名字更易读，字号 13→14px、id 9→10px），连接轨道 `CONN` 64→36px，连接条长度跟随变窄（`BAR = CONN-8`），周期 `PITCH` 162→166px。
- **占位格无效化样式**：偶数-偶数交叉处的无意义占位格改为斜纹底纹 + 虚线边框（弱化透明度），明确标示「无效占位、不可交互」，与地块 / 连接块区分。
- **空连接块不可点击创建连接**：连接块只有存在连接时可点击编辑；空连接块（`conn-block-empty`）不再响应点击，去掉连接块编辑面板里的「补建该走向的连接」——创建连接只从地块面板发起（方向 + 目的地可自选，延伸出去）；空块 title 提示「创建连接请先点击地块」。

### 🐛 修复编辑视图地块全部堆叠在第一列（绝对定位缺失 + 画布双重缩放）

- **修复「所有地块都在第一列」**：绝对定位画布渲染改为由 `.loc-cell` / `.conn-block` / `.ph-cell` 各自 `position: absolute` 定位（此前为静态/相对定位，JS 设置的 `left/top` 不生效，地块格按普通流堆叠成单列）；`.world-canvas` 改为绝对定位左上角锚定。
- **修复画布双重缩放**：画布布局尺寸不再乘 zoom（此前布局尺寸与 `transform: scale(zoom)` 叠加导致缩放时滚动范围与网格线失真）；视觉缩放全部由 transform 承担。
- **网格线对齐**：网格背景周期改为未缩放 PITCH，并把背景原点偏移到内容原点（PAD）——网格线在任意缩放级别下都与地块 / 连接轨道边界对齐。

### 🐛 修复编辑视图找不到已有地块 + 限制空白延伸

- **修复「找不到已有地块」**：视口与内容无交集时自动回到内容（`ensureContentVisible`），内容变小或视图 hidden 状态下滚动位置错乱后重绘会兜底居中，保证已有地块总在可视范围内。
- **空白延伸有限制**：取消画布空白随滚动无限增长的机制，改为内容边界四周**固定留白**（800px）——画布尺寸只由内容决定，不新增地块时滚动范围有限；点击内容边界外的空地块（HALO）或网格背景新建地块后，内容生长、画布随之扩展。

### 🛠 编辑视图无限画布 + 地图缩放

- **可滚动画布**：编辑视图改为绝对定位画布渲染（替代固定 CSS Grid 表格），内容四周固定留白；内容边界外渲染一圈空地块（HALO），编辑模式点击即可新建 → 世界向外生长，点击网格背景可就近任意槽位新建。
- **地图缩放**：Ctrl/⌘+滚轮（含触控板捏合）以光标为中心缩放，工具条提供 − / + / 百分比（点击重置 100%）/「适应」按钮；缩放范围 20%–400%，画布网格线随缩放保持清晰。
- **平移**：普通滚轮滚动平移（沿用 #graph 滚动容器），跨重绘 / 模式切换保留视口位置。

### 🛠 编辑视图重构：交替网格 + 详情栏（取消地块分身）

- **交替网格表格**：行与列均按「连接块 · 地块 · 连接块」交替排布——奇数轨为地块（正方形，只显示名字与 id + 出生点徽标），偶数轨为连接块（长方形，横向/纵向）；偶数-偶数交叉处为无意义的小占位格。地块主位仍由 layout 整数坐标决定。
- **连接只画方向线条**：连接块内以 SVG 方向条表达连接状态——单向箭头 / 双向双箭头 / 空块=不连接，不绘制单独的方向文字；多边同向合并为一条、双向合并为双箭头条。
- **取消地块分身，改为特殊连接**：非相邻连接（目的地可自选）不再生成分身格，而是以虚线强调条显示在**出发方向的连接块内**；环路 / 捷径 / 跨区连接均以此呈现。
- **右侧详情栏**：可收起 / 展开；点击地块、连接块、方向条或空地块，在栏内显示并编辑详细信息。
- **取消「新建地块 / 新建出口」按钮，改为查看 / 编辑子模式**：查看模式只读；编辑模式点击**空地块**可新建地块、点击**地块**可创建连接（方向 + 目的地可自选，方向指向相邻地块时自动选中该目标）、点击已存在的连接可编辑连接、点击连接块可补建该走向的连接（方向固定）。
- 沙箱兼容：详情栏为页内面板（自绘弹窗仅保留确认 / 错误提示），无原生 alert。

### 🛠 玩家视图目标格缩小 + 长名换行

- **目标格缩小**：目标格不再占满棋盘 1/3 的块，改为内容自适应收缩并居中（`place-items: center`），减小内边距与字号，十字排布更紧凑。
- **长名允许换行**：目标名与中心地块名取消省略号截断，超长时在格内换行（`overflow-wrap: anywhere`），适配移动端窄屏。

### 🐛 修复视图切换失效

- **修复两个视图同时渲染 / 无法切换到玩家视图**：`#view-edit` / `#view-play` 在 CSS 中设置了 `display: flex`，作者样式覆盖 UA 的 `[hidden] { display: none }`，导致 `hidden` 属性失效——加载时玩家视图作为空容器叠在编辑视图下方，切到玩家模式时编辑视图也不消失。新增全局 `[hidden] { display: none !important; }` 兜底（`#play-error` 的 `display: flex` 同隐患一并覆盖）。

### 🛠 玩家视图重构

- **玩家视图布局重构**：地图 div 中间是只含地块名称的小块（移除说明文本与玩家 id），上下左右直接连接 1 跳可达地块；当前地块说明文本移到与地图 div 平级的独立信息 div（实体系统后续版本再接入）。
- **编辑防冲突**：新建出口时方向默认选「出发地块的第一个空闲方向」，切换出发地块若方向冲突自动改选，避免无意中造出同方向冲突（减少「+N」折叠的误触发）。

### 🛠 编辑器视图重构（网格表格 + 地块分身）

- **编辑模式彻底改为网格表格**：地块按其整数网格坐标（列 / 行）落在表格主格，**所有连接必须相邻**——出口方向决定目标相邻格，目标主位不在该格时在该格显示目标的地块分身（虚线框、可叠多行）；反向边用相反方向（A 右是 B ⇔ B 左是 A），捷径 / 环路这类特殊关系以分身呈现。
- **分身可收起**：每个分身可折叠成出发地块格内的标签（点击展开）；工具栏「收起/展开全部分身」一键切换。
- **出生点徽标**：编辑视图不再显示 agent / 玩家当前位置，改为在出生点地块（agent 与玩家注册起始）显示徽标；`GET /world/state` 新增 `spawn` 字段。
- **方向自动推荐**：新建出口时按「① 反向边存在 → 其反方向；② 目标相邻 → 相对位置推导；③ 否则首个空闲方向」自动推荐；切换出发 / 目标地块时重新推荐。
- **表单调整**：地块表单的布局 x/y 改为「列 / 行」整数输入，新建时默认落在空闲格（优先最近点选地块的邻格）。

### 🛠 玩家视图精简与响应式布局 + 编辑网格修复

- **玩家视图 1 跳目标只显示地块名**：移除目标格内的出口标签等详细信息，只保留名字（隐藏目标仍为 `???`）。
- **玩家视图响应式布局**：PC web 端左侧地图、右侧详情；移动端上面地图、下面详情。
- **修复编辑视图网格渲染**：`.edit-grid` 补 `display: grid` 与 `gap`，消除此前各格纵向堆叠（`gridColumn`/`gridRow` 未生效）与方向标签错位问题，编辑视图呈现真正的表格 / 网格地图。

### 🛠 玩家视图布局优化（动态填充 + 分栏不滚动）

- **目标格缩小**：1 跳目标格与中心小块整体缩小，目标名超长省略号截断，地图更紧凑。
- **地图 div 动态填充**：棋盘正方形尺寸取「地图区域宽/高较小值」并随区域变化自适应（ResizeObserver），地图区域填满剩余空间。
- **响应式分栏且整体不滚动**：PC 左侧地图、右侧详情一整列（详情内部滚动）；移动端上面地图、下面详情；整个玩家视图视口内不滚动，仅详情列可单独滚动。
- **页面骨架改为固定视口高度**：`body` 固定 `100dvh` 不滚动；编辑视图工具栏固定、图区域内部滚动。

## [v0.2.0] - 2026-08-10

### 🎮 可视化编辑与玩家视图（Milestone）

- **出口方向槽位**：`Exit` 新增 `direction` 字段（`up/right/down/left` 之一），同出发地块的出边方向互异——多边同目标分占不同十字槽位、不重合；老库自动迁移（`ALTER TABLE`），schema 升 v2。
- **地图可视化编辑**：引擎新增 6 个编辑动作（地块 / 出口的创建、更新、删除）；删除地块级联删除相关出边、拒绝删除有玩家（含 agent）占据的地块；自环出口合法；布局坐标支持显式清空。
- **Web API 编辑端点**：新增 `POST /world/location/{create,update,delete}` 与 `POST /world/exit/{create,update,delete}`。
- **调试页重构为单页双模式**（模块化拆分为 5 个 ES module）：
  - **编辑模式**：全图可视化（上帝视角，地块永远真名）+ 可视化编辑——点击节点 / 边进入表单，设置名称、描述、布局坐标、出口方向与「隐藏目的地」开关；边画方向信息（单向箭头 / 双向双箭头）；重名地块悬浮全体高亮。
  - **玩家模式**：当前地块 + 有出边连接的 1 跳目标十字布局——无连接的槽位不可见；所有边一视同仁（无箭头简单连线，不区分单向 / 双向）；无回环（自环出口照常占槽位）；隐藏目标显示 `???`；违规地图（出度 >4 或同方向冲突）以「+N」折叠兜底。
- **约束分层**：数据层不设出度限制（合法 = 结构允许），规范由可视化编辑器保证，视图层以「+N」折叠兜底违规地图。
- CI js-check 覆盖全部 5 个前端模块。

## [v0.1.0] - 2026-08-10

### 🎉 初始版本（Milestone）

- **有向图世界底座**：地块（Location）为节点、带标签出口（Exit）为有向边——`a→b` 可达不蕴含 `b→a`；支持多边同目标、隐藏目标（`reveal_target=False`，场景显示 `???`）与环路，图数据结构允许抽象、高复杂度的结构。
- **LLM 工具**：agent 化身经 `world_look` / `world_move` 查看场景与移动，位置跨对话持久化（SQLite）。
- **框架内置调试页**（`pages/world/`）：SVG 有向图可视化（布局坐标优先、确定性兜底布局）、按出口 id 移动、当前地块高亮与 agent 静态标记。
- **Web API**：`GET /world/state`、`POST /world/player/register`、`POST /world/move`、`POST /world/player/deregister`。
- **持久化**：SQLite（aiosqlite + WAL，启动全量载入内存）；空库幂等播种示例小镇 + 迷雾区（多边同目标 / 隐藏目标 / 环路示例）；agent 位置写回、人类玩家仅内存且超时清理。
- **协议无关引擎**：`world/` 纯 Python 引擎不依赖框架，动作层被 LLM 工具 / 插件页 API / 未来世界 HTTP API 共用，为 MCP 封装预留。
- **工程化**：CI 工作流（`pytest.yml` 单元测试 + 前端 JS 语法检查、`ruff-format.yml` 格式与质量检查）；插件名统一为「世界编辑器」。

### 📝 文档与工程化（Docs & Chores）

- 补齐 LICENSE（AGPL-3.0）、README、CONTRIBUTING、CODE_OF_CONDUCT、CHANGELOG、issue/PR 模板与 dependabot。
