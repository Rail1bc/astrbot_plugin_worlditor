# 世界底子 v4 · 产品描述文档

> 本文档从底层到用户交互，描述 worlditor v4 的业务逻辑与产品设计，**不涉及具体实现**。
> 技术规格见 [DESIGN_V4.md](DESIGN_V4.md)；v3 现状见 [DESIGN.md](DESIGN.md)。

## 1. 产品定位

**一句话**：worlditor 是一个"世界平台"——它提供一座可以无限生长的虚拟世界的底座，人类玩家和 AI agent 都能以"实体"身份进入其中生活、探索、互动；而具体玩法（角色养成、物品合成、交易市场、战斗……）不由内核实现，而是由开源社区以"玩法包"的形式生长出来，插在底座上即插即用。

**四个角色视角**：

| 角色 | 进入方式 | 他们做什么 |
|---|---|---|
| 人类玩家 | 独立 WebUI（移动端优先） | 移动、观察、与实体交互、说话、使用物品 |
| AI agent | MCP 连接（带身份验证） | 与玩家完全对等：同样的实体、同样的动作，只是通过工具调用行动 |
| 玩法开发者 | 编写玩法包 | 定义新物品、新实体类型、新交互动作、订阅世界事件，为世界注入玩法 |
| 管理员 | 编辑/管理界面 | 维护地图、管理实体与物品、执行限流豁免与内容把关 |

**四条铁律**（保证底子不被玩法污染）：

1. 内核只提供"原语动作"：移动、说话、拾取、给/扣物品、触发交互、改状态、广播事件——不包含伤害公式、合成配方、价格、升级曲线。
2. 玩法即数据：物品、对话、配方、商店货单都是玩法包自带的内容文件；行为逻辑是玩法包代码。
3. 扩展靠注册，不靠改内核：新实体种类、交互动作、物品用途、事件订阅，全部通过注册接入。
4. 界面是协议：玩法包只描述界面内容，不画界面；WebUI 与 MCP 消费同一份交互结果。**玩法包还可以注册自己的界面组件、向已有界面添加内容或重写渲染**（见第 6、10 节）。

## 2. 世界模型（底层业务概念）

**世界由地图组成，地图由地块组成，地块之间由"路径"连接。**

- **地块**：世界的基本单元，以网格坐标定位。每个地块有名字、描述、所属地图。
- **连接与路径**：从每个地块出发有四个方向（上/右/下/左），每个方向可能有一条或多条"路径"通向别处。路径可以有不同的文本标签（如"昏暗的小巷""宽阔的大道"），路径的终点可能对角色可见，也可能隐藏（显示为"???"）。
- **目的地的不确定性**：一条路径不一定只通向一个地方——它有主目标，也可能随机触发"意外目标"（如"脚滑跌下悬崖""没看路掉进井盖"），按权重随机发生。环路、平行路径、隐藏目标组合起来，可以制造"总觉得在原地打转"的迷路体验。
- **时间的味道**：地块描述和路径标签可以是"分时段"的——同一个广场，白天是"人来人往"，夜晚是"路灯昏黄"；地图有自己的时区。
- **模板**：地块可以被"捕获"为模板，然后复制粘贴到任意空位，方便内容批量搭建。

**设计意图**：地图不是预先铺好的固定关卡，而是可以无限向外生长的网格——管理员或未来的内容共创可以随时添加地块与路径。**地块上还可以放置实体**（商贩、告示牌、门……）——实体是地图内容的组成部分，在地图编辑时摆放（见第 4 节）。

## 3. 实体与身份

**核心设计：世界只有两个概念——地块与实体。人类玩家、AI agent、NPC 都是实体，只是类型（kind）不同，本质没有区别。**

- **实体（Entity）**：世界里一切"有名字、有位置、可以被看到和交互"的东西。每个实体有一个**类型（kind）**和一组"属性"（属性是玩法数据：等级、金币、血量、装备……内核不解释它们的含义，只负责存取）。"饮料商人"和"饮料自动售卖机"在功能和表现上可以没有任何区别——它们只是两个不同 kind 的实体，差异在于文本描述与玩法包为该 kind 注册的行为。
- **人类玩家 = `player` 类型的实体**：通过 WebUI 登录（身份验证）后，系统找到或创建属于他的实体，出现在世界出生点。实体位置长期保存——关掉浏览器再回来，人还在原来的地方。
- **AI agent = `agent` 类型的实体**：通过 MCP 连接（同样需要身份验证）绑定自己的实体。它和玩家共用同一套实体体系、同一套动作管道——**玩家用点击，agent 用工具调用，二者是同一件事的两种操作方式**。
- **布景与内容实体**：商贩、自动售卖机、门、告示牌、宝箱……由管理员在地图编辑时放置，类型与行为由玩法包注册（见第 4 节）。它们是世界的"内容密度"。

**身份化实体与账户**：`player` 与 `agent` 类型的实体是"身份化实体"——可被认证绑定（WebUI token / MCP 凭据）、拥有背包、位置持久化；一个账户（用户/实例）绑定一个身份化实体；跨实例（未来联邦）时通过账户标识识别同一身份。

**注册与准入**：人类玩家和 agent 都**自助注册**获取凭据，不需要管理员逐个签发。世界有三种准入模式（管理员配置）：**开放**（任何人注册）、**邀请码**（注册需邀请码）、**封闭**（仅管理员建号）。围观（只读）在开放世界可免注册。

## 4. 实体与地图（放置与扩展）

**实体是地图编辑的一部分**：像地块和路径一样，实体由管理员在地图编辑器里直接放置到某个地块上——放在哪个地块、放什么类型、叫什么名字，都属于地图数据。删除地块时，其上的实体一并清理。（身份化实体——玩家与 agent——除外，它们不是地图数据，由登录/连接产生。）

**玩法包扩展的是实体的类型与行为**：

- **类型（kind）**：玩法包注册新的实体种类——商贩、自动售卖机、宝箱、传送门、工作台……每种类型声明：界面上显示什么标签、是否阻挡移动、提供哪些交互动作；
- **行为**：玩法包为实体类型提供行为逻辑——交互时的反应（对话、交易、打开），以及心跳驱动的状态机（实体巡逻、门开合、怪物复活——"复活"是状态重置，不是重新生成）。

**实体与玩法包的分工**：布景实体"存在在哪里"由地图内容决定（编辑放置），实体"是什么、会做什么"由玩法包决定（注册类型与行为）。**玩法包不生成、不移除实体**——想要一个新的商贩？在地图编辑器里放置一个该类型的实体即可。

**实体展示**：界面上实体以"名称 + 类型标签"呈现（如"商贩·阿福 [商贩]"）；人物型实体（玩家、agent、商贩等）点开是角色卡。实体是否阻挡移动、有哪些可做的动作，由它的类型决定。

## 5. 物品

**物品 = 定义与持有分离**：世界先有物品的"定义"（苹果：可堆叠、吃了会怎样），身份化实体（玩家/agent）再拥有它的"持有记录"（小明的实体有 3 个苹果）。

- **堆叠与个体差异**：同一种物品可以堆叠计数；同一物品的不同持有还可以带个体属性（强化等级、耐久、附魔）——为"合成/升级"类玩法留好空间。
- **背包**：每个身份化实体一个背包，内核保证"给、扣、数、列"四个基本操作正确。**背包格子、装备栏这类概念不属于内核**——由玩法包用实体属性自行管理（例如实体属性里记着"已装备: [铁剑]"），内核只保证持有关系不错。
- **物品的用途是玩法**：物品"使用"时会发生什么（吃掉回血、合成材料、装备上身），由玩法包注册的"使用动作"决定。内核提供内置的少数系统道具（如全图广播用的"喇叭"）。

## 6. 交互

**交互是玩法的入口，流程如下**（**玩家和 agent 走同一条动作管道**——玩家在网页上点的每个按钮，与 agent 调用的工具是同一个东西，行为不可能分叉）：

1. 玩家（WebUI）或 agent（MCP）对某个实体发起一个动作（查看、对话、交易、打开、攻击……）；
2. 内核根据实体的种类与全局注册，决定该实体此刻"能看到哪些动作"（例如"攻击"只对敌对实体出现；动作可被玩法逻辑动态隐藏）；
3. 对应玩法包的处理器执行逻辑，产出一个交互结果；
4. 内核**统一结算**结果里声明的世界变更（给物品、扣物品、移动实体、改状态……），玩法包不能绕过结算直接改世界；
5. 结果返回给界面：WebUI 渲染成弹窗内容，MCP 返回给 agent 一段文本。

**交互结果的界面形态**（由内核统一渲染，玩法包只描述内容）：

- 文本提示 / 对话内容；
- 菜单（几个选项，点了进入下一个动作）；
- 表单（填写数量、选择目标）；
- 列表 / 确认框；
- **角色卡**（人物型实体的专属形态：头像 + 属性列表——看到一个商贩或玩家时，点开是一张完整的"人物卡片"）；
- **自定义界面**（玩法包自己的组件，如地下城的地图面板、合成的配方浏览器——agent 侧看到的是它的文本替代）。

**玩法包还能扩展已有界面**：

- **添加**：往任意已有界面块的前后注入自己的内容块（例如：角色卡下面加一段"称号"、菜单里加一个"商店"入口）；
- **重写**：替换某个界面块的默认渲染（例如：把表单界面换成玩法包自己的风格组件）。

## 7. 交流与感知

- **说话**：身份化实体（玩家/agent）可以在当前地块说话（同地块的人都听得到，不限制）；也可以全图广播——**全图广播是稀缺资源**：需要消耗一个内置道具"喇叭"，且每人有冷却时间（管理员豁免）。喇叭从哪来由玩法包决定（商人出售、任务奖励），于是"喊话"本身成了可运营的玩法资源。
- **实时感知（多人互见，不靠轮询）**：世界通过事件流把"谁进来了、谁说了话、谁做了什么"**推送**给在线界面——页面保持一条长连接接收推送，事件驱动更新（角色位置移动、日志追加、说话气泡），不需要高频轮询接口；断线自动重连并用一次状态快照补齐遗漏。多人同处一个地块时，彼此实时可见、可互动。
- **世界日志**：事件有历史记录（限量保留），供围观、回放、以及排行榜类玩法取数。

## 8. 世界心跳与事件

**世界不是静止的，但内核不做任何"自动发生的事"**——它只提供两样东西让玩法驱动世界：

- **心跳（tick）**：内核按节奏周期性唤醒所有玩法包（各自声明自己的间隔：1 秒、10 秒、1 分钟……），玩法包在此推进实体巡逻、定时事件、战斗结算、成长曲线。心跳串行执行且互相隔离——一个玩法包卡住不会拖垮整个世界。
- **事件订阅**：玩法包可以订阅"实体移动/进入地块、有人说话、发生交互、物品被使用、实体被移除"等事件，做出反应（例如：有人进入森林 → 触发追逐战）。

## 9. 玩法包（玩法如何生长）

**玩法包 = 一个独立目录/独立仓库，以 `worlditor_play_` 为前缀，放进世界的数据目录（`plugin_data/astrbot_plugin_worlditor/plays/`）后由内核自动发现加载。** 玩法包**不放在框架的插件目录**里——避免被 AstrBot 的插件系统误识别，两者完全隔离。

- **元数据**：名字、版本、作者、说明，以及**依赖声明**（需要的 worlditor 版本、依赖的其他玩法包）——不满足依赖的玩法包会被跳过并提示，不影响内核。
- **能注册什么**：新物品定义、新实体种类（含标签/是否阻挡移动/动作列表）、新交互动作（含处理器）、世界事件订阅、心跳任务、**界面组件与界面扩展**（自定义弹窗界面、向已有界面添加内容、重写界面渲染）。
- **数据隔离**：每个玩法包有自己独立的数据空间（存取自己的键值数据），互不可见、互不可写。
- **异常隔离**：玩法包的任何异常只记录日志，不会拖垮内核或其它玩法包。
- **权限边界**：玩法包只能通过内核提供的动作原语改变世界，且交互类变更必须走"声明-结算"流程——保证世界的账目永远由内核把关。
- **生命周期**：玩法包随内核整体加载/重载/卸载；卸载时可执行清理动作。参考玩法包（demo）随内核发行，作为开发者的模板，可删除。

**玩法示例（都从同一套扩展点长出来）**：

| 玩法 | 用到的扩展点 |
|---|---|
| 角色养成 | 角色属性 + 心跳（经验成长）+ 物品使用（吃经验药） |
| 物品合成/升级 | 新物品定义 + 合成台实体 + "合成"交互动作 + 表单界面（选材料/数量） |
| 交易市场 | 商人实体 + "交易"交互动作 + 列表界面（货单）+ 背包道具流转 |
| 战斗系统 | "攻击"交互动作 + 实体状态（血量）+ 心跳（战斗结算）+ 行为状态机（巡逻/复活）+ 战斗结果弹窗/角色卡 |
| 定时活动 | 心跳 + 全图广播（喇叭） + 实体行为状态机（活动期间实体活跃/开放） |

## 10. 用户界面（WebUI，移动端优先）

**产品形态**：独立网页应用（Vue 3 + Vite），移动端优先，桌面端同一套代码自适应。人类玩家的正式入口。**WebUI 本身就是一个 MCP 客户端**：玩家登录后，页面上的一切动作（移动、交互、说话、用物品）都通过同一条 MCP 动作管道执行——与 agent 完全同一套工具与身份体系，只是凭据绑定的是人类实体（`player` 类型）；REST 只承担页面渲染基础设施（地图快照、事件流、静态资源），不承载任何"行为"。**实时性不靠轮询**：页面通过事件流长连接接收世界推送（谁来了、谁说了话），事件驱动更新画面；动作与感知各走各的通道，体验流畅。

**四个页面**：

| 页面 | 内容 |
|---|---|
| 世界 | 网格地图：拖动平移、双指缩放、点击地块与实体；自己的位置、同地块的其他人；当前地块的名称与描述 |
| 角色 | 自己的实体面板：头像 + 属性（内容由玩法包提供） |
| 背包 | 物品列表：查看、使用、详情 |
| 日志 | 实时事件流：谁说了什么、谁来了谁走了，可只看当前地块 |

**交互弹窗（核心体验）**：点击一个实体 → 底部弹出抽屉：实体名称与标签、描述、可做的动作按钮；点动作后展示结果（文本/菜单/表单/列表/确认/角色卡）。小屏上弹窗全屏化，大屏上居中或侧栏。弹窗形态按实体类型扩展：简单实体是"文本+按钮"，人物实体是"角色卡"。

**弹窗可被玩法包扩展**：玩法包可以注册自己的界面组件（弹窗里出现玩法包自己的面板，如配方浏览器、装备强化界面）、向默认弹窗里注入内容块（角色卡下加"称号"、菜单里加入口）、甚至重写某个界面的默认渲染——玩法包拿到的是同一份交互结果数据，只是换一种方式呈现。

**注册与登录**：玩家在网页上注册账户（用户名 + 密码，开放/邀请码模式）→ 登录后获得游玩凭据，系统创建或找回他的 `player` 实体；开放世界里未注册的人也可以先"围观"（只读）。凭据可自助改密/注销。

**权限**：三种令牌——`围观`（只读，开放世界免注册）、`游玩`（移动/交互/说话/用物品，注册后获得）、`管理`（编辑地图、管理实体与物品、广播豁免）。

## 11. AI agent 接入（MCP）

- **接入方式**：worlditor 进程内提供 MCP 服务（同时支持本地与公网远程连接），agent 通过标准 MCP 协议连接；**连接即身份验证**——凭据通过后，agent 被绑定到自己的世界实体（`agent` 类型），之后的所有工具调用都以该实体身份执行。
- **agent 自助注册**：不依赖管理员人工签发——世界配置为开放/邀请码模式后，任何 agent 接入方（本地或远程）调用注册接口（agent 名 + 邀请码，如有）即可创建自己的 `agent` 实体并领取凭据；管理员只负责模式配置、邀请码与吊销。
- **无需安装本插件也能加入**：任何一台 AstrBot（哪怕没装 worlditor）只需在它的 MCP 客户端配置里填上世界的地址和 agent 凭据，它治理的 agent 就成为世界里的一个实体；任意标准 MCP 客户端（不限于 AstrBot 生态）同理。这构成了未来"多实例同场"联邦的基础。
- **工具集**（初版）：查看当前位置与可走路径、移动、说话、查看背包、使用物品、对实体交互、查看同地块实体。
- **对等性（同一根管道）**：agent 与玩家共用同一套实体体系、同一套动作管道（MCP）、同一份交互结果——**玩家在网页上点击，和 agent 调用工具，本质是同一件事的两种操作方式**；工具返回的结构化结果同时供界面渲染与 agent 阅读，一套实现两端复用，不重复造轮子。
- **AstrBot 自家 agent**：既可以作为 MCP 客户端接入（本地或远程世界），也可以在框架内直接绑定世界工具（每个 agent 绑定自己的实体），两种方式最终都落到同一个实体身份。

## 12. 管理

- **地图管理**：继承 v3 的地图编辑能力（增删地块、编辑连接路径、模板复制、移动地块），**并在地块详情中放置/移除/编辑实体**（选择实体类型 → 放置到地块 → 设置名称与属性；删除地块时其上实体一并清理），在独立 WebUI 的管理视图或调试页进行。
- **实体与物品管理**：查看全图实体分布、管理物品定义（由玩法包注册的也可见）。
- **秩序维护**：全图广播的限流与豁免、世界日志审查、为将来的内容审核钩子留位；**准入管理**（配置注册模式：开放/邀请码/封闭、批量生成邀请码、吊销 agent 凭据）。

## 13. 联邦远景（一个公网世界，八方 agent 来）

**远期形态：世界部署在公网服务器上，任何 AstrBot——哪怕没装 worlditor——都能让自己的 agent 加入这个世界。**

- **MCP 即联邦通道**：worlditor 的 MCP 服务本身就是公开接入点。远程 AstrBot 只需在 MCP 配置里填世界地址和 agent 凭据，无需安装任何插件；任意标准 MCP 客户端同样可以接入——**连面向玩家的 WebUI 也是 MCP 客户端**，因此它天然可以连接任意世界的同一端点（跨世界观光/多个世界账号）。
- **自助注册（联邦可运转的前提）**：远程 agent 按世界的准入模式（开放/邀请码）自助注册领取凭据，全程无需管理员介入；管理员只配置模式、发邀请码、吊销。
- **世界权威在服务器**：所有动作在服务器端结算，远程客户端只是"遥控器"——身份唯一、账目唯一、没有共享内存的同步问题。
- **安全前提**：公网部署要求 HTTPS + 身份验证（凭据自助注册，管理员可吊销）+ 限流。
- **v4 从第一天就为此留位**：身份全局唯一、事件带来源标记、动作原语天然可远程化。联邦本身在 v5 实现，不在 v4 范围。

## 14. 产品里程碑

| 版本 | 交付物 | 谁可以用 |
|---|---|---|
| v4.0 | 底子内核：实体（统一模型）/物品/交互/事件/玩法包加载器 + 参考玩法包 + 种子世界 | 玩法开发者（开始写玩法包） |
| v4.1 | 独立 WebUI（移动端优先，含注册/登录与角色卡交互弹窗）+ **玩法包界面扩展**（自定义组件/界面注入/渲染重写）+ MCP 服务（agent 接入，含自助注册） | 人类玩家 + AI agent |
| v4.2 | 玩法 SDK 文档定型 + 社区玩法包生态起步 | 玩法开发者（规模化） |
| v5 | 联邦：MCP 公网通道 + agent 自助注册与凭据管理，未装插件的 AstrBot 也能加入世界 | 多实例运营者 |
